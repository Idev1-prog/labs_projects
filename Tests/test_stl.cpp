#include "pch.h"
#include "stl.h"
#include <vector>
/*
TEST(STLusing, check_sort) {
	std::vector<float> v1 = { 1.4, 3, 7.1, 0, 15.3 };
	//for (int i = 0; i < v1.size(); i++) {
	//	std::cout << v1[i] << " ";
	//}
	//std::cout << std::endl;
	ASSERT_NO_THROW(sorting(v1));
	for (int i = 1; i < v1.size(); i++) {
		EXPECT_TRUE(v1[i - 1], v1[i]);
	}
}

*/